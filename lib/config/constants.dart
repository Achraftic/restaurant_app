import 'package:flutter/material.dart';

class AppColors {
  // Primary Theme Colors
  static const primary = Color(0xFF6D4C41); // Chocolate brown
  static const secondary = Color(0xFFFF7043); // Coral orange

  // Light Theme Colors
  static const backgroundLight = Color(0xFFFFFBFA); // Soft off-white
  static const surfaceLight = Color(0xFFFFFFFF); // Pure white
  static const textPrimaryLight = Color(0xFF3E2723); // Deep brown text
  static const onPrimaryLight = Color(0xFFFFFFFF); // White on primary
  static const onSecondaryLight = Color(0xFF3E2723); // Dark text on secondary

  // Dark Theme Colors
  static const backgroundDark = Color(0xFF1E1E1E); // Almost black
  static const surfaceDark = Color(0xFF2C2C2C); // Dark grey
  static const textPrimaryDark = Color(0xFFE0E0E0); // Light grey text
  static const onPrimaryDark = Color(0xFFFFFFFF); // White on primary
  static const onSecondaryDark = Color(0xFF000000); // Black on secondary
}

class AppStrings {
  static const appName = 'My App';
  static const loginTitle = 'Welcome Back';
}
